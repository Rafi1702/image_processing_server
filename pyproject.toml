[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my_project"
version = "0.1.0"
requires-python = ">=3.9.6"
dependencies = [
    "scikit-learn>=1.0",
    "pandas>=2.0",
    "opencv-python>=4.9.0"
]
